interface:
  display_name: "Recursive Language Models"
  short_description: "Handle huge-context tasks using the RLM (tool + recursive subagent) workflow"
  default_prompt: "Apply the Recursive Language Models workflow: keep full context on disk, map with tools, delegate chunk reading to subagents/agent teams, and synthesize with evidence."
